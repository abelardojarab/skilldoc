#!/bin/sh
# This script was created by make-mzscheme-launcher

# {{{ bindir
bindir="/p/advda/elisp/doctool/mzscheme//bin"
# }}} bindir

exec "${bindir}/mzscheme" -N "$0" '-qge' '(require (lib "slatex-launcher.ss" "slatex"))'  ${1+"$@"}
